# security-intel-mcp
Security intelligence MCP server for AI agents: CVE lookup (NVD), package vulns (OSV), URL threat check (URLhaus), IP reputation (AbuseIPDB). One dependency-free Cloudflare Worker.
